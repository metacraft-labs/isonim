# Docs theme ↔ brand-token divergences

This document has moved to the shared **CodeTracer docs design system**, next to
the tokens it describes:

> `codetracer-design-system/docs/DESIGN-DIVERGENCES.md`

The docs design tokens now live in one shared source of truth
(`codetracer-design-system/docs/codetracer-docs.tokens.json`), consumed by every
Metacraft docs site (isonim/docs/users, isonim-docs/site, codetracer/docs/book-isonim)
and the design-system editor. Edit the tokens and the divergence catalogue there.

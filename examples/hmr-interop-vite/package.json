{
  "name": "isonim-hmr-interop-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Reference integration: isonim HMR plugging into Vite's native HMR pipeline.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "vite": "^5.4.0"
  }
}

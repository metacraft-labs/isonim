## Defines all page component procs for test fixtures.
## Since bracket-named files ([id].nim) cannot be imported as Nim modules,
## component procs are defined here and imported by the test.

proc IndexPage*() = discard
proc AboutPage*() = discard
proc IdPage*() = discard
proc PostsPage*() = discard

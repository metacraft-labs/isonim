// Phase I/J (2026-05-28) — OBSOLETE STUB.
//
// The per-chat robot strip + trailing "+" button used to live in
// the chrome bar's right-edge slot, opening a slide-out drawer for
// the clicked chat session. Both are gone. Chat sessions now live
// in the right-sidebar AI assistant panel as a traditional tab
// strip (Phase J — ``renderAiAssistantPanel`` in ``shell.nim``).
//
// The replacement live coverage is
// ``tests/browser/e2e_editor_ai_tabs_live.mjs``.

import test from "node:test";

test.skip("e2e_chat_tabs_initial_state_has_single_new_chat: OBSOLETE — chrome-bar robot strip removed", () => {});
test.skip("e2e_chat_tabs_plus_creates_new_chat_and_switching_works: OBSOLETE — chrome-bar robot strip removed", () => {});
test.skip("e2e_chat_tabs_many_chats_scroll_horizontally: OBSOLETE — chrome-bar robot strip removed", () => {});
